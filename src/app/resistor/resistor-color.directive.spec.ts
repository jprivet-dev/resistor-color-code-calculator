import { ResistorColorDirective } from './resistor-color.directive';

describe('ResistorColorDirective', () => {
  it('should create an instance', () => {
    const directive = new ResistorColorDirective();
    expect(directive).toBeTruthy();
  });
});
